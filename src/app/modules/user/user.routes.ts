import { Routes } from '@angular/router';
import {HomeComponent } from './home/home.component';

export const USER_ROUTES: Routes = [
  { path: '', component: HomeComponent }, // Default user route
];
